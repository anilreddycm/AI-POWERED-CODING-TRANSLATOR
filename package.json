{
  "name": "ai-powered-code-translator",
  "version": "1.0.0",
  "description": "Fullstack AI-Powered Code Translator (Translate, Analyze, Explain & Optimize)",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "install-server": "cd server && npm ci --no-audit --no-fund",
    "install-client": "cd client && npm ci --include=dev --no-audit --no-fund",
    "install-all": "npm run install-server && npm run install-client",
    "build-client": "cd client && npm run build",
    "build": "npm run install-all && npm run build-client",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
